










var navBar = document.querySelector(".bar")
window.onscroll = function () {
    if (window.scrollY >= 50) {
        navBar.classList.add("background-bar")
    }
}


























